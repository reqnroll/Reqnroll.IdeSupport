# Reqnroll.IdeSupport documentation (local preview root)

This page exists only so the `docs/site/` tree builds standalone with Sphinx
for local preview. It is **not** synced to docs.reqnroll.net — only
`ide-support/` is. See [README.md](README.md) for the sync model.

```{toctree}
:maxdepth: 2

ide-support/index
```
