/**
 * Neither language id has an SDK-provided constant: `csharp` is just the conventional id the C#
 * extension registers, and `gherkin` is this project's own id declared in `package.json`.
 */
export const CSHARP_LANGUAGE_ID = 'csharp';
export const GHERKIN_LANGUAGE_ID = 'gherkin';
